package com.websocket.client.connection;

/**
 * Represents com.websocket.client.connection states e.g. connected and disconnected.
 */
public enum ConnectionState {
    CONNECTING, CONNECTED, DISCONNECTING, DISCONNECTED, ALL
}
